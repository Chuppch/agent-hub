# 根据agent_id 查看 对应的 client
select * from ai_agent_flow_config where agent_id = 3;

# 根据client_id 查看 具体的 client信息
select *
from ai_client
where client_id in (3101, 3102, 3103,3104);

# 根据client_id 查看 对应的 具体的配置信息
select *
from ai_client_config
where ai_client_config.source_id in (3101, 3102, 3103,3104);


# select *
# from ai_client_config
# where ai_client_config.source_id in (3101, 3102, 3103,3104);

# 查看具体的配置信息，查找需要用的 target_type 和 target_id
select *
from ai_client_config
where ai_client_config.source_id = 3101;

# 根据具体的 target_type 和 target_id 查找具体的表以及 具体的信息
select *
from ai_client_model
where model_id = 3001;
select *
from ai_client_system_prompt
where prompt_id = 6101;
select *
from ai_client_advisor
where advisor_id = 4001;
select *
from ai_client_tool_mcp
where mcp_id = 5006;

